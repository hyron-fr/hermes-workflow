{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "coherence",
  "type": "object",
  "required": ["status", "verdict"],
  "properties": {
    "status": { "type": "string", "enum": ["ok", "ko"] },
    "verdict": { "type": "string" }
  }
}
