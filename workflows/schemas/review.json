{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rubrique de review P0–P3 (artefact de l'étape agentique)",
  "type": "object",
  "required": ["findings"],
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["severity", "title"],
        "properties": {
          "severity": {
            "enum": ["P0", "P1", "P2", "P3"],
            "description": "P0 blocant (≥1 -> block) / P1 sérieux (>=1 -> flag) / P2 mineur / P3 nit"
          },
          "title": { "type": "string", "minLength": 1 },
          "location": { "type": "string", "description": "fichier:ligne ou chemin" },
          "evidence": { "type": "string", "description": "preuve : extrait de code, sortie de test, reproduction. Obligatoire pour P0/P1." },
          "repro": { "type": "string", "description": "commande de reproduction (alternative à evidence). Obligatoire pour P0/P1." },
          "fix_suggestion": { "type": "string" }
        }
      }
    },
    "summary": { "type": "string" }
  }
}
